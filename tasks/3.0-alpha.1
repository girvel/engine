  3.0-alpha.1

I'm quite excited about implementing chunked grids.

I also wonder if it would be better to stop with engine as a submodule and instead just do engine as a folder; the git history is probably SO fucked right now, the engine submodule wasn't properly synchronized at all. On the other hand, I would lose all of the development history.
